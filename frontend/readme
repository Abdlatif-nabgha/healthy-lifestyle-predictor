# Frontend - HealthVibe AI

## 👨‍💻 Développé par
**Personne 4** - Frontend Developer (Next.js)

## 🎨 Description
Interface moderne et interactive pour la prédiction de santé basée sur le mode de vie. Application Next.js avec design violet/rose inspiré du mockup HealthVibe AI.

## ✨ Fonctionnalités

### Interface utilisateur
- ✅ Design moderne avec dégradés violet/rose/bleu
- ✅ Header avec logo animé et titre "HealthVibe AI"
- ✅ Slogan : "Your Personal Lifestyle Intelligence"
- ✅ 5 inputs interactifs avec sliders personnalisés
- ✅ Cartes avec effet glassmorphism
- ✅ Animations et transitions fluides

### 5 Paramètres de santé
- 🌙 **Sleep Hours** (Heures de sommeil) : 3-12h
- 💪 **Exercise Time** (Temps d'exercice) : 0-180 min
- ❤️ **Heart Rate** (Fréquence cardiaque) : 40-120 bpm
- 💧 **Water Intake** (Consommation d'eau) : 0-5 L
- 🍎 **Calorie Intake** (Apport calorique) : 1000-4000 kcal

### Intégration API
- ✅ Connexion à l'API Flask via fetch
- ✅ Envoi des données au format JSON
- ✅ Gestion des erreurs de connexion
- ✅ État de chargement avec spinner animé
- ✅ Affichage des résultats en temps réel

### Affichage des résultats
- ✅ Carte verte pour "Healthy Lifestyle" 🎉
- ✅ Carte rouge pour "Needs Improvement" ⚠️
- ✅ Pourcentage de confiance de la prédiction
- ✅ Message personnalisé du modèle
- ✅ 3-4 recommandations adaptées au résultat
- ✅ Icônes et styling conditionnels

## 🚀 Installation

### Prérequis
- Node.js 18+ installé
- npm ou yarn
- API Flask en cours d'exécution sur le port 5000

### Étapes
```bash
# 1. Aller dans le dossier frontend
cd frontend

# 2. Installer les dépendances
npm install

# 3. Lancer en mode développement
npm run dev
```

L'application sera accessible sur : **http://localhost:3000**

## 🔗 Configuration API

### Endpoint Flask
```
POST http://localhost:5000/predict
```

### Format de requête
```json
{
  "sleepHours": 7,
  "exerciseTime": 30,
  "heartRate": 75,
  "waterIntake": 2,
  "calorieIntake": 2000
}
```

### Format de réponse attendu
```json
{
  "probability": 0.85,
  "label": 1,
  "message": "Your lifestyle indicators suggest a healthy pattern!"
}
```

**Note** : 
- `label: 1` = Healthy (carte verte)
- `label: 0` = Needs improvement (carte rouge)

## 🛠️ Technologies utilisées

- **Next.js 14** - Framework React avec App Router
- **React 18** - Bibliothèque UI
- **Tailwind CSS** - Framework CSS utility-first
- **Lucide React** - Bibliothèque d'icônes modernes
- **Fetch API** - Appels HTTP vers Flask

## 📱 Responsive Design

L'interface s'adapte à toutes les tailles d'écran :
- 📱 Mobile : 320px+
- 📱 Tablet : 768px+
- 💻 Desktop : 1024px+

## 🎨 Design System

### Palette de couleurs
- **Background** : Dégradé violet foncé → noir
- **Violet** : `#A855F7` (Purple-500)
- **Rose** : `#EC4899` (Pink-500)
- **Bleu** : `#3B82F6` (Blue-500)
- **Vert** : `#10B981` (Green-500) - Healthy
- **Rouge** : `#EF4444` (Red-500) - Warning

### Composants
- Cartes avec backdrop-blur et transparence
- Sliders personnalisés avec glow effect
- Bouton gradient animé au hover
- Cartes de résultats conditionnelles
- Badges colorés pour les valeurs

## 📦 Structure des fichiers
```
frontend/
├── app/
│   ├── page.js          # Composant principal HealthVibe AI
│   ├── layout.js        # Layout global avec metadata
│   └── globals.css      # Styles Tailwind + animations CSS
├── public/              # Assets statiques (images, fonts)
├── package.json         # Dépendances npm
├── package-lock.json    # Lock des versions
├── next.config.js       # Configuration Next.js
├── tailwind.config.js   # Configuration Tailwind
├── postcss.config.js    # Configuration PostCSS
├── .gitignore          # Fichiers à ignorer par Git
└── README.md           # Ce fichier
```

## 🔄 Intégration avec le backend

Assurez-vous que l'API Flask (développée par Personne 3) est lancée avant de tester :
```bash
# Dans un autre terminal
cd backend
python app.py
```

L'API doit accepter les requêtes CORS depuis `http://localhost:3000`.

## 🐛 Troubleshooting

### Erreur : "Erreur de connexion à l'API"
**Cause** : L'API Flask n'est pas lancée ou CORS mal configuré

**Solution** :
```bash
# Vérifier que Flask tourne
curl http://localhost:5000/predict

# Vérifier les CORS dans Flask
from flask_cors import CORS
CORS(app)
```

### Erreur : Les styles ne s'affichent pas
**Cause** : Tailwind mal configuré ou 'use client' manquant

**Solution** :
- Vérifier que `'use client';` est en première ligne de `page.js`
- Relancer le serveur : `npm run dev`

### Erreur : Module not found
**Cause** : Dépendances non installées

**Solution** :
```bash
npm install
```

## 📊 Features avancées (optionnel)

Pour améliorer l'application :
- [ ] Historique des prédictions (localStorage)
- [ ] Graphiques de progression (Recharts)
- [ ] Export des résultats en PDF
- [ ] Mode sombre/clair
- [ ] Animations plus avancées (Framer Motion)
- [ ] Validation des inputs
- [ ] Tests unitaires (Jest)

## 🧪 Tests
```bash
# Lancer les tests (si implémentés)
npm test

# Build de production
npm run build

# Lancer en production
npm start
```

## 📸 Screenshots

[Vous pouvez ajouter des captures d'écran ici]

### Interface principale
![Screenshot principal](./screenshots/main.png)

### Résultat Healthy
![Screenshot healthy](./screenshots/healthy.png)

### Résultat Needs Improvement
![Screenshot warning](./screenshots/warning.png)

## 🤝 Intégration avec l'équipe

Ce frontend fait partie du projet **HealthVibe AI** développé en équipe :

- **Personne 1** : Dataset synthétique (300 lignes, 5 features)
- **Personne 2** : Modèle ML from scratch (régression logistique)
- **Personne 3** : API Flask (endpoint /predict)
- **Personne 4** : Frontend Next.js (vous êtes ici !)

## 📝 Notes pour l'équipe

### Pour Personne 3 (Backend) :
Votre API doit retourner exactement ce format :
```json
{
  "probability": float,  // Entre 0 et 1
  "label": int,          // 0 ou 1
  "message": string      // Message descriptif
}
```

### Pour tester l'intégration complète :
1. Lancer le backend (port 5000)
2. Lancer le frontend (port 3000)
3. Remplir le formulaire
4. Cliquer sur "Analyze My Health"
5. Vérifier que les résultats s'affichent correctement

## 📄 Licence

Projet académique - Tous droits réservés

## 👤 Auteur

Développé par **[Votre Nom]** - Frontend Developer

---

**Date de création** : Novembre 2024  
**Dernière mise à jour** : Novembre 2024  
**Version** : 1.0.0